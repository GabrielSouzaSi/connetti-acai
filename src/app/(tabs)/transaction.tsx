import { Text, View } from "react-native"

export default function TabTransactionScreen() {
	return (
		<View className="flex-1 items-center justify-center">
			<Text className="font-bold text-2xl">Negociações</Text>
		</View>
	)
}
